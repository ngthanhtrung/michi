(function (factory) {
    if (typeof define === 'function' && define.amd) {
        define({{#if amdModuleId}}'{{amdModuleId}}', {{/if}}factory);

    } else if (typeof exports === 'object') {
        factory(require, exports, module);
    }

}(function (require, exports, module) {
{{{code}}}
}));
